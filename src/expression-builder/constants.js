const EQ = '=';
const PLUS = '+';
const MINUS = '-';
const LT = '<';
const GT = '>';
const LE = '<=';
const GE = '>=';

const AND = 'AND';
const OR = 'OR';
const BETWEEN = 'BETWEEN';

const GROUP_START = '(';
const GROUP_END = ')';

const SET = 'SET';
const REMOVE = 'REMOVE';
const ADD = 'ADD';
const DELETE = 'DELETE';

const PREFIX = 'eb';

module.exports = {
    EQ,
    PLUS,
    MINUS,
    LT,
    GT,
    LE,
    GE,
    AND,
    OR,
    BETWEEN,
    GROUP_START,
    GROUP_END,
    SET,
    REMOVE,
    ADD,
    DELETE,
    PREFIX,
};

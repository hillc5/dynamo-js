const EQ = '=';
const PLUS = '+';
const MINUS = '-';
const LT = '<';
const GT = '>';
const LE = '<=';
const GE = '>=';

const AND = 'AND';
const OR = 'OR';
const BETWEEN = 'BETWEEN';

const GROUP_START = '(';
const GROUP_END = ')';

const SET = 'SET';
const REMOVE = 'REMOVE';
const ADD = 'ADD';
const DELETE = 'DELETE';

const PREFIX = 'eb';

const LIST_INDEX_REG = /\[\d+\]/g;

const STRING = 'S';
const STRING_SET = 'SS';
const NUMBER = 'N';
const NUMBER_SET = 'NS';
const BINARY = 'B';
const BINARY_SET = 'BS';
const BOOLEAN = 'BOOL';
const NULL = 'NULL';
const LIST = 'L';
const MAP = 'M';

module.exports = {
    EQ,
    PLUS,
    MINUS,
    LT,
    GT,
    LE,
    GE,
    AND,
    OR,
    BETWEEN,
    GROUP_START,
    GROUP_END,
    SET,
    REMOVE,
    ADD,
    DELETE,
    PREFIX,
    LIST_INDEX_REG,
    STRING,
    STRING_SET,
    NUMBER,
    NUMBER_SET,
    BINARY,
    BINARY_SET,
    BOOLEAN,
    NULL,
    LIST,
    MAP,
};
